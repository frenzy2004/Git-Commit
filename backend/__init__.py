"""Runtime package for the git commit classroom-access prototype.

The modules inside this package are intentionally split by boundary:
HTTP routing, upload policy, model adapters, text shaping, and device
transport live in separate files so the demo path and hardware path can be
tested independently.
"""
