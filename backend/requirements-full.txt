# Optional stack for live model calls.
# Keep this separate so the default demo install stays light.
-r requirements.txt

# Claude client for image understanding and transcript compression.
anthropic>=0.69.0,<1.0.0

# Local speech-to-text engine used when MOCK_TRANSCRIBE=false.
faster-whisper>=1.0.3,<2.0.0
