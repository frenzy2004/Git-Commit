# Optional stack for live model calls.
# Keep this separate so the default demo install stays light.
-r requirements.txt

# OpenAI client for image understanding, transcript compression, and speech-to-text.
openai>=1.99.0,<3.0.0
